# TLS lo termina Nginx Proxy Manager (NPM); Caddy solo enruta en HTTP.
:80 {
  encode gzip

  handle_path /api/* {
    reverse_proxy api:8000
  }

  handle /health {
    reverse_proxy api:8000
  }

  handle {
    reverse_proxy web:80
  }
}
